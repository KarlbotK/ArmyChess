services:
  server:
    build: ./server
    restart: unless-stopped
    environment:
      ALLOWED_ORIGINS: http://localhost:8088

  web:
    build: ./web
    restart: unless-stopped
    depends_on:
      - server
    ports:
      - "8088:80"
