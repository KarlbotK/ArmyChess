# Product Design QA

final result: passed

验收日期：2026-09-20  
浏览器：Codex in-app browser（Chromium）  
状态：四人到齐后的正式布阵阶段，以及提交后的轮流行棋

## 对照证据

- 棋盘结构真值：`docs/design/classic-board-reference.png`（1687×1246）
- 视觉真值：`docs/design/selected-modern-tabletop.png`（1487×1058）
- 实现截图：`docs/audit/frontend/layout-rules-1280x720.png`（1280×720）
- 同屏对照：`docs/audit/frontend/qa-comparison-rules-fix.png`
- CSS 视口：1280×720；浏览器 device pixel ratio 1.6875；截图已由浏览器归一化为 1280×720 像素。
- 同屏对照将三张图分别归一化为 640×452，仅用于比较整体构图；实现截图另以原始尺寸检查文字和控件。

## 验收动作

- 创建房间并让另外三个真实浏览器会话加入；
- 四人到齐后停留在布阵阶段，没有自动跳过；
- 己方 25 枚棋子全部显示身份，军旗使用醒目红色棋面；
- 点击军旗与司令交换位置后，“确认布阵”立即禁用；
- 重新随机后所有规则恢复通过；
- 四方分别确认布阵后进入对局；
- 连续实际行棋验证顺序为南家 → 西家 → 北家 → 东家 → 南家；
- 打开规则弹窗，检查布阵、大小、铁路、行营、司令阵亡亮旗、胜负和暗棋说明；
- 控制台 `error`/`warn`：0。

## 五项保真检查

- 字体与层级：沿用第二版的宋体展示标题和清晰无衬线正文；布阵标题、规则状态和主按钮层级明确。
- 间距与构图：棋盘仍是视觉中心，规则卡放在右侧空区，不遮挡己方棋子、玩家牌或聊天入口。
- 颜色与令牌：保持深绿毡面、暖白卡片、克制金色；军旗使用红色作为规则识别色，并保留文字标签，颜色不是唯一线索。
- 图片与素材：继续使用已选桌面背景；棋盘结构取自经典参考，矩形棋子是遵循用户参考的有意偏离。
- 文案与内容：布阵卡和规则弹窗明确写出军旗、炸弹、地雷、行营、四方轮转和司令阵亡亮旗；没有“谁击败了谁”的身份映射。

## 聚焦区域说明

布阵规则卡与己方阵地在 1280×720 原始截图中均可完整阅读，军旗、按钮禁用状态和五条规则无需额外放大裁切即可判断，因此没有再生成局部裁图。

## Findings

- P0：0
- P1：0
- P2：0
- P3：窄于横屏平板的竖屏完整对局仍不在 MVP 范围。

## 比较历史

### 第一轮

- [P1] 四人到齐后自动提交阵型，跳过军棋的核心布阵过程。
- [P1] 规则弹窗缺少炸弹首排、军旗大本营、司令阵亡亮旗和明确轮转顺序。
- [P1] 军旗与普通己方棋子视觉差异不足。
- [P1] 顶栏没有持续显示南西北东的执棋顺序。

### 修复

- 增加点击两枚棋子交换、重新随机、实时规则检查和显式确认提交；
- 非法布阵禁用提交，服务端继续执行第二次权威校验；
- 军旗改为带文字的红色棋面，对手军旗仍只在司令阵亡后公开；
- 顶栏持续显示“南 → 西 → 北 → 东”；
- 规则弹窗扩展为八个完整规则区块。

### 第二轮

同屏证据 `docs/audit/frontend/qa-comparison-rules-fix.png` 显示整体风格仍与第二版一致；实际四窗口验收确认布阵约束、提交门禁和四方轮转均可用。没有剩余 P0/P1/P2。

